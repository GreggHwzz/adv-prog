import Navbar from "@/components/NavBar";

const Settings = () => {
  return (
    <div>
      <Navbar role="ADMIN"/>
      {/* Autres composants ou contenu de la page */}
    </div>
  );
};

export default Settings;